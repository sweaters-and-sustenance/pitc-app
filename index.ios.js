/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
  AppRegistry
} from 'react-native';
import wrapper from './wrapper'

AppRegistry.registerComponent('PITApp', () => wrapper);
