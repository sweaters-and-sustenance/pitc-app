import {
  StyleSheet
} from 'react-native';
import {
  COLOR_GRAY
} from '../constants';

const inputs = StyleSheet.create({
  base: {
    height: 40,
    borderRadius: 3,
    borderColor: COLOR_GRAY,
    borderWidth: 1,
    alignSelf: 'stretch',
    marginBottom: 10,
    padding: 10,
    backgroundColor: '#FFF'
  },
});
export default inputs;
