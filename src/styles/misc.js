import {
  StyleSheet
} from 'react-native';
import {
  COLOR_OFF_WHITE
} from '../constants';

const misc = StyleSheet.create({
  container: {
    backgroundColor: COLOR_OFF_WHITE
  }
});
export default misc;
